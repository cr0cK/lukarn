# CLAUDE.md

Claude Code loads this file. The instructions themselves live in `AGENTS.md` —
the tool-agnostic name, kept canonical so an agent that reads it directly gets
the same content this shim imports.

@AGENTS.md
