{
  "name": "lukarn",
  "version": "1.0.0",
  "private": true,
  "license": "AGPL-3.0-only",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "pnpm --parallel --filter \"./packages/*\" dev",
    "build": "pnpm --filter @lukarn/shared build && pnpm --filter @lukarn/web build && pnpm --filter @lukarn/server build",
    "start": "pnpm --filter @lukarn/server start",
    "typecheck": "pnpm -r typecheck",
    "lint": "eslint .",
    "format": "prettier --write .",
    "test": "pnpm -r test",
    "hash-password": "pnpm --filter @lukarn/server hash-password",
    "create-admin": "pnpm --filter @lukarn/server create-admin",
    "reset-password": "pnpm --filter @lukarn/server reset-password",
    "check:format": "prettier --check .",
    "check:specs": "node tools/check-specs.mjs",
    "check:changelog": "node tools/check-changelog.mjs",
    "check:links": "node tools/check-links.mjs",
    "verify": "pnpm typecheck && pnpm lint && pnpm check:format && pnpm test && pnpm check:specs && pnpm check:links && pnpm check:changelog",
    "prepare": "git config core.hooksPath .githooks || true"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "eslint": "^9.17.0",
    "globals": "^17.9.0",
    "prettier": "^3.4.2",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.66.0"
  },
  "packageManager": "pnpm@11.6.0"
}
